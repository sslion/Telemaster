APP.plug1 =  {
    hello(text) {
        console.log(">>> " + text + " <<<");
    }
}

APP.add.user = {
    name : '',
    age : 0,
    addLastName() {}
}
APP.user.onCClisk() = fu
app.addLastName = addLastName

app = {
    addListener(temp) {
        temp.addListener('click', () => {
            addLastName()
        })
    }
}

// uiClick